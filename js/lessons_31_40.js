/* Lessons Extra 31-40 */
const LessonsExtra31_40 = [
{id:31,difficulty:4,title:{python:{en:'Factory & Singleton Pattern',cz:'Tovární metoda a Singleton'},csharp:{en:'Factory & Singleton Pattern',cz:'Tovární metoda a Singleton'}},description:{python:{en:'Learn creational design patterns in Python',cz:'Nauč se tvořivé návrhové vzory v Pythonu'},csharp:{en:'Learn creational design patterns in C#',cz:'Nauč se tvořivé návrhové vzory v C#'}},content:{python:{en:'<h1>Factory & Singleton Pattern</h1><p>Design patterns are proven solutions to common software problems. The <strong>Factory</strong> pattern creates objects without specifying exact classes. The <strong>Singleton</strong> ensures only one instance exists.</p><h2>Factory Function</h2><p>A factory function returns different objects based on input:</p><pre><code class="language-python">class Dog:\n    def speak(self):\n        return \"Woof!\"\n\nclass Cat:\n    def speak(self):\n        return \"Meow!\"\n\ndef animal_factory(animal_type):\n    animals = {\"dog\": Dog, \"cat\": Cat}\n    cls = animals.get(animal_type.lower())\n    if cls is None:\n        raise ValueError(f\"Unknown animal: {animal_type}\")\n    return cls()\n\npet = animal_factory(\"dog\")\nprint(pet.speak())  # Woof!</code></pre><h2>Singleton with __new__</h2><p>Override <code>__new__</code> to control instance creation:</p><pre><code class="language-python">class Database:\n    _instance = None\n\n    def __new__(cls):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n            cls._instance.connection = \"Connected\"\n        return cls._instance\n\ndb1 = Database()\ndb2 = Database()\nprint(db1 is db2)          # True\nprint(db1.connection)       # Connected</code></pre><h2>Abstract Factory</h2><pre><code class="language-python">class Button:\n    def render(self):\n        return \"Generic Button\"\n\nclass DarkButton(Button):\n    def render(self):\n        return \"Dark Button\"\n\nclass LightButton(Button):\n    def render(self):\n        return \"Light Button\"\n\ndef ui_factory(theme):\n    if theme == \"dark\":\n        return DarkButton()\n    return LightButton()\n\nbtn = ui_factory(\"dark\")\nprint(btn.render())</code></pre>',cz:'<h1>Tovární metoda a Singleton</h1><p>Návrhové vzory jsou osvědčená řešení běžných problémů. <strong>Tovární metoda</strong> vytváří objekty bez určení přesné třídy. <strong>Singleton</strong> zajišťuje, že existuje jen jedna instance.</p><h2>Tovární funkce</h2><p>Tovární funkce vrací různé objekty podle vstupu:</p><pre><code class="language-python">class Pes:\n    def mluv(self):\n        return \"Haf!\"\n\nclass Kocka:\n    def mluv(self):\n        return \"Mňau!\"\n\ndef tovarna_zvirat(typ):\n    zvirata = {\"pes\": Pes, \"kocka\": Kocka}\n    cls = zvirata.get(typ.lower())\n    if cls is None:\n        raise ValueError(f\"Neznámé zvíře: {typ}\")\n    return cls()\n\nmazlicek = tovarna_zvirat(\"pes\")\nprint(mazlicek.mluv())  # Haf!</code></pre><h2>Singleton s __new__</h2><p>Přepíšeme <code>__new__</code> pro řízení vytváření instancí:</p><pre><code class="language-python">class Databaze:\n    _instance = None\n\n    def __new__(cls):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n            cls._instance.pripojeni = \"Připojeno\"\n        return cls._instance\n\ndb1 = Databaze()\ndb2 = Databaze()\nprint(db1 is db2)          # True\nprint(db1.pripojeni)       # Připojeno</code></pre><h2>Abstraktní továrna</h2><pre><code class="language-python">class Tlacitko:\n    def vykresli(self):\n        return \"Obecné tlačítko\"\n\nclass TmaveTlacitko(Tlacitko):\n    def vykresli(self):\n        return \"Tmavé tlačítko\"\n\ndef ui_tovarna(tema):\n    if tema == \"tmave\":\n        return TmaveTlacitko()\n    return Tlacitko()\n\nbtn = ui_tovarna(\"tmave\")\nprint(btn.vykresli())</code></pre>'},csharp:{en:'<h1>Factory & Singleton Pattern</h1><p>Design patterns are proven solutions to common software problems. The <strong>Factory</strong> pattern creates objects without specifying exact classes. The <strong>Singleton</strong> ensures only one instance exists.</p><h2>Static Factory Method</h2><p>A factory method returns different objects based on input:</p><pre><code class="language-csharp">abstract class Animal {\n    public abstract string Speak();\n}\nclass Dog : Animal {\n    public override string Speak() { return \"Woof!\"; }\n}\nclass Cat : Animal {\n    public override string Speak() { return \"Meow!\"; }\n}\n\nclass AnimalFactory {\n    public static Animal Create(string type) {\n        switch (type.ToLower()) {\n            case \"dog\": return new Dog();\n            case \"cat\": return new Cat();\n            default: throw new ArgumentException(\"Unknown: \" + type);\n        }\n    }\n}\n\nAnimal pet = AnimalFactory.Create(\"dog\");\nConsole.WriteLine(pet.Speak());  // Woof!</code></pre><h2>Sealed Singleton</h2><p>Use a <code>sealed</code> class with a private constructor:</p><pre><code class="language-csharp">sealed class Database {\n    private static Database _instance;\n    public string Connection { get; private set; }\n\n    private Database() {\n        Connection = \"Connected\";\n    }\n\n    public static Database Instance {\n        get {\n            if (_instance == null)\n                _instance = new Database();\n            return _instance;\n        }\n    }\n}\n\nvar db1 = Database.Instance;\nvar db2 = Database.Instance;\nConsole.WriteLine(db1 == db2);       // True\nConsole.WriteLine(db1.Connection);   // Connected</code></pre><h2>Generic Factory</h2><pre><code class="language-csharp">class Factory&lt;T&gt; where T : new() {\n    public static T Create() {\n        return new T();\n    }\n}\n\nvar dog = Factory&lt;Dog&gt;.Create();\nConsole.WriteLine(dog.Speak());</code></pre>',cz:'<h1>Tovární metoda a Singleton</h1><p>Návrhové vzory jsou osvědčená řešení běžných problémů. <strong>Tovární metoda</strong> vytváří objekty bez určení přesné třídy. <strong>Singleton</strong> zajišťuje jedinou instanci.</p><h2>Statická tovární metoda</h2><pre><code class="language-csharp">abstract class Zvire {\n    public abstract string Mluv();\n}\nclass Pes : Zvire {\n    public override string Mluv() { return \"Haf!\"; }\n}\nclass Kocka : Zvire {\n    public override string Mluv() { return \"Mňau!\"; }\n}\n\nclass TovarnaZvirat {\n    public static Zvire Vytvor(string typ) {\n        switch (typ.ToLower()) {\n            case \"pes\": return new Pes();\n            case \"kocka\": return new Kocka();\n            default: throw new ArgumentException(\"Neznámé: \" + typ);\n        }\n    }\n}\n\nZvire mazlicek = TovarnaZvirat.Vytvor(\"pes\");\nConsole.WriteLine(mazlicek.Mluv());</code></pre><h2>Zapečetěný Singleton</h2><pre><code class="language-csharp">sealed class Databaze {\n    private static Databaze _instance;\n    public string Pripojeni { get; private set; }\n\n    private Databaze() {\n        Pripojeni = \"Připojeno\";\n    }\n\n    public static Databaze Instance {\n        get {\n            if (_instance == null)\n                _instance = new Databaze();\n            return _instance;\n        }\n    }\n}\n\nvar db1 = Databaze.Instance;\nvar db2 = Databaze.Instance;\nConsole.WriteLine(db1 == db2);       // True</code></pre><h2>Generická továrna</h2><pre><code class="language-csharp">class Tovarna&lt;T&gt; where T : new() {\n    public static T Vytvor() {\n        return new T();\n    }\n}</code></pre>'}}},
{id:32,difficulty:4,title:{python:{en:'Observer & Strategy Pattern',cz:'Vzor Observer a Strategy'},csharp:{en:'Observer & Strategy Pattern',cz:'Vzor Observer a Strategy'}},description:{python:{en:'Learn behavioral design patterns in Python',cz:'Nauč se behaviorální návrhové vzory v Pythonu'},csharp:{en:'Learn behavioral design patterns in C#',cz:'Nauč se behaviorální návrhové vzory v C#'}},content:{python:{en:'<h1>Observer & Strategy Pattern</h1><p>The <strong>Observer</strong> pattern lets objects subscribe to events. The <strong>Strategy</strong> pattern lets you swap algorithms at runtime.</p><h2>Observer Pattern</h2><p>An event system with callback lists:</p><pre><code class="language-python">class EventEmitter:\n    def __init__(self):\n        self._listeners = {}\n\n    def on(self, event, callback):\n        if event not in self._listeners:\n            self._listeners[event] = []\n        self._listeners[event].append(callback)\n\n    def emit(self, event, *args):\n        for cb in self._listeners.get(event, []):\n            cb(*args)\n\nemitter = EventEmitter()\nemitter.on(\"data\", lambda msg: print(f\"Got: {msg}\"))\nemitter.on(\"data\", lambda msg: print(f\"Log: {msg}\"))\nemitter.emit(\"data\", \"Hello!\")  # Both fire</code></pre><h2>Strategy Pattern</h2><p>Pass different callables to change behavior:</p><pre><code class="language-python">def sort_by_name(items):\n    return sorted(items, key=lambda x: x[\"name\"])\n\ndef sort_by_price(items):\n    return sorted(items, key=lambda x: x[\"price\"])\n\nclass Store:\n    def __init__(self, strategy):\n        self.strategy = strategy\n        self.products = []\n\n    def add(self, name, price):\n        self.products.append({\"name\": name, \"price\": price})\n\n    def display(self):\n        for p in self.strategy(self.products):\n            print(f\"{p[\"name\"]}: ${p[\"price\"]}\")\n\nstore = Store(sort_by_price)\nstore.add(\"Banana\", 1)\nstore.add(\"Apple\", 2)\nstore.display()</code></pre><h2>Strategy with Classes</h2><pre><code class="language-python">class UpperFormatter:\n    def format(self, text):\n        return text.upper()\n\nclass LowerFormatter:\n    def format(self, text):\n        return text.lower()\n\nclass Printer:\n    def __init__(self, formatter):\n        self.formatter = formatter\n    def print_text(self, text):\n        print(self.formatter.format(text))\n\np = Printer(UpperFormatter())\np.print_text(\"hello\")  # HELLO</code></pre>',cz:'<h1>Vzor Observer a Strategy</h1><p><strong>Observer</strong> umožňuje objektům odebírat události. <strong>Strategy</strong> umožňuje měnit algoritmy za běhu.</p><h2>Vzor Observer</h2><p>Systém událostí se seznamem callbacků:</p><pre><code class="language-python">class Vysilac:\n    def __init__(self):\n        self._posluchaci = {}\n\n    def registruj(self, udalost, callback):\n        if udalost not in self._posluchaci:\n            self._posluchaci[udalost] = []\n        self._posluchaci[udalost].append(callback)\n\n    def vysli(self, udalost, *args):\n        for cb in self._posluchaci.get(udalost, []):\n            cb(*args)\n\nv = Vysilac()\nv.registruj(\"data\", lambda zprava: print(f\"Přijato: {zprava}\"))\nv.registruj(\"data\", lambda zprava: print(f\"Log: {zprava}\"))\nv.vysli(\"data\", \"Ahoj!\")</code></pre><h2>Vzor Strategy</h2><p>Předej různé funkce pro změnu chování:</p><pre><code class="language-python">def razeni_dle_jmena(polozky):\n    return sorted(polozky, key=lambda x: x[\"jmeno\"])\n\ndef razeni_dle_ceny(polozky):\n    return sorted(polozky, key=lambda x: x[\"cena\"])\n\nclass Obchod:\n    def __init__(self, strategie):\n        self.strategie = strategie\n        self.produkty = []\n\n    def pridej(self, jmeno, cena):\n        self.produkty.append({\"jmeno\": jmeno, \"cena\": cena})\n\n    def zobraz(self):\n        for p in self.strategie(self.produkty):\n            print(f\"{p[\"jmeno\"]}: {p[\"cena\"]} Kč\")\n\nobchod = Obchod(razeni_dle_ceny)\nobchod.pridej(\"Banán\", 20)\nobchod.pridej(\"Jablko\", 15)\nobchod.zobraz()</code></pre><h2>Strategy s třídami</h2><pre><code class="language-python">class VelkaFormater:\n    def formatuj(self, text):\n        return text.upper()\n\nclass Tiskarna:\n    def __init__(self, formater):\n        self.formater = formater\n    def tiskni(self, text):\n        print(self.formater.formatuj(text))\n\nt = Tiskarna(VelkaFormater())\nt.tiskni(\"ahoj\")  # AHOJ</code></pre>'},csharp:{en:'<h1>Observer & Strategy Pattern</h1><p>The <strong>Observer</strong> pattern uses delegates and events for notifications. The <strong>Strategy</strong> pattern uses interfaces to swap algorithms.</p><h2>Observer with Events</h2><p>C# has built-in event support with delegates:</p><pre><code class="language-csharp">class EventEmitter {\n    public event Action&lt;string&gt; OnData;\n\n    public void Emit(string message) {\n        OnData?.Invoke(message);\n    }\n}\n\nvar emitter = new EventEmitter();\nemitter.OnData += msg => Console.WriteLine($\"Got: {msg}\");\nemitter.OnData += msg => Console.WriteLine($\"Log: {msg}\");\nemitter.Emit(\"Hello!\");  // Both handlers fire</code></pre><h2>Strategy with Interfaces</h2><p>Define a strategy interface and swap implementations:</p><pre><code class="language-csharp">interface ISortStrategy {\n    List&lt;string&gt; Sort(List&lt;string&gt; items);\n}\n\nclass AlphaSort : ISortStrategy {\n    public List&lt;string&gt; Sort(List&lt;string&gt; items) {\n        var sorted = new List&lt;string&gt;(items);\n        sorted.Sort();\n        return sorted;\n    }\n}\n\nclass ReverseSort : ISortStrategy {\n    public List&lt;string&gt; Sort(List&lt;string&gt; items) {\n        var sorted = new List&lt;string&gt;(items);\n        sorted.Sort();\n        sorted.Reverse();\n        return sorted;\n    }\n}\n\nclass Sorter {\n    private ISortStrategy _strategy;\n    public Sorter(ISortStrategy s) { _strategy = s; }\n    public void Execute(List&lt;string&gt; items) {\n        foreach (var i in _strategy.Sort(items))\n            Console.WriteLine(i);\n    }\n}\n\nvar data = new List&lt;string&gt; {\"Banana\", \"Apple\", \"Cherry\"};\nnew Sorter(new AlphaSort()).Execute(data);</code></pre><h2>Custom Delegate Observer</h2><pre><code class="language-csharp">delegate void Notify(string msg);\n\nclass Publisher {\n    public Notify Subscribers;\n    public void Send(string msg) {\n        Subscribers?.Invoke(msg);\n    }\n}\n\nvar pub = new Publisher();\npub.Subscribers += m => Console.WriteLine($\"A: {m}\");\npub.Subscribers += m => Console.WriteLine($\"B: {m}\");\npub.Send(\"Update!\");</code></pre>',cz:'<h1>Vzor Observer a Strategy</h1><p><strong>Observer</strong> v C# využívá delegáty a události. <strong>Strategy</strong> používá rozhraní pro výměnu algoritmů.</p><h2>Observer s událostmi</h2><p>C# má vestavěnou podporu událostí přes delegáty:</p><pre><code class="language-csharp">class Vysilac {\n    public event Action&lt;string&gt; PriDatech;\n\n    public void Vysli(string zprava) {\n        PriDatech?.Invoke(zprava);\n    }\n}\n\nvar v = new Vysilac();\nv.PriDatech += z => Console.WriteLine($\"Přijato: {z}\");\nv.PriDatech += z => Console.WriteLine($\"Log: {z}\");\nv.Vysli(\"Ahoj!\");</code></pre><h2>Strategy s rozhraním</h2><pre><code class="language-csharp">interface IStrategie {\n    List&lt;string&gt; Serad(List&lt;string&gt; polozky);\n}\n\nclass AbecedniRazeni : IStrategie {\n    public List&lt;string&gt; Serad(List&lt;string&gt; polozky) {\n        var s = new List&lt;string&gt;(polozky);\n        s.Sort();\n        return s;\n    }\n}\n\nclass Razeni {\n    private IStrategie _strategie;\n    public Razeni(IStrategie s) { _strategie = s; }\n    public void Proved(List&lt;string&gt; data) {\n        foreach (var i in _strategie.Serad(data))\n            Console.WriteLine(i);\n    }\n}\n\nvar data = new List&lt;string&gt; {\"Banán\", \"Jablko\", \"Třešeň\"};\nnew Razeni(new AbecedniRazeni()).Proved(data);</code></pre><h2>Vlastní delegát</h2><pre><code class="language-csharp">delegate void Oznameni(string zprava);\n\nclass Vydavatel {\n    public Oznameni Odberatele;\n    public void Posli(string z) {\n        Odberatele?.Invoke(z);\n    }\n}\n\nvar v2 = new Vydavatel();\nv2.Odberatele += z => Console.WriteLine($\"A: {z}\");\nv2.Posli(\"Aktualizace!\");</code></pre>'}}},
{id:33,difficulty:3,title:{python:{en:'Advanced Error Handling',cz:'Pokročilé ošetření chyb'},csharp:{en:'Advanced Error Handling',cz:'Pokročilé ošetření chyb'}},description:{python:{en:'Custom exceptions and context managers',cz:'Vlastní výjimky a kontextové manažery'},csharp:{en:'Custom exceptions and IDisposable',cz:'Vlastní výjimky a IDisposable'}},content:{python:{en:'<h1>Advanced Error Handling</h1><p>Go beyond basic try/except. Create your own exceptions and use context managers for resource management.</p><h2>Custom Exceptions</h2><p>Create exception classes by inheriting from <code>Exception</code>:</p><pre><code class="language-python">class ValidationError(Exception):\n    def __init__(self, field, message):\n        self.field = field\n        super().__init__(f\"{field}: {message}\")\n\nclass AgeError(ValidationError):\n    pass\n\ndef set_age(age):\n    if age &lt; 0:\n        raise AgeError(\"age\", \"Cannot be negative\")\n    if age > 150:\n        raise AgeError(\"age\", \"Unrealistic value\")\n    return age\n\ntry:\n    set_age(-5)\nexcept AgeError as e:\n    print(f\"Validation failed: {e}\")\n    print(f\"Field: {e.field}\")</code></pre><h2>Context Managers (with/as)</h2><p>Use <code>with</code> for automatic resource cleanup:</p><pre><code class="language-python">class FileManager:\n    def __init__(self, filename, mode):\n        self.filename = filename\n        self.mode = mode\n        self.file = None\n\n    def __enter__(self):\n        self.file = open(self.filename, self.mode)\n        return self.file\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        if self.file:\n            self.file.close()\n        return False  # Do not suppress exceptions\n\nwith FileManager(\"test.txt\", \"w\") as f:\n    f.write(\"Hello!\")</code></pre><h2>contextlib Shortcut</h2><pre><code class="language-python">from contextlib import contextmanager\n\n@contextmanager\ndef timer():\n    import time\n    start = time.time()\n    yield\n    elapsed = time.time() - start\n    print(f\"Elapsed: {elapsed:.3f}s\")\n\nwith timer():\n    total = sum(range(1000000))\n    print(f\"Sum: {total}\")</code></pre><div class="warning"><p><strong>Warning:</strong> Always use context managers for files, locks, and database connections to avoid resource leaks.</p></div>',cz:'<h1>Pokročilé ošetření chyb</h1><p>Jdi dál za základní try/except. Vytvoř si vlastní výjimky a používej kontextové manažery pro správu prostředků.</p><h2>Vlastní výjimky</h2><p>Vytvoř třídy výjimek děděním z <code>Exception</code>:</p><pre><code class="language-python">class ChybaValidace(Exception):\n    def __init__(self, pole, zprava):\n        self.pole = pole\n        super().__init__(f\"{pole}: {zprava}\")\n\nclass ChybaVeku(ChybaValidace):\n    pass\n\ndef nastav_vek(vek):\n    if vek &lt; 0:\n        raise ChybaVeku(\"vek\", \"Nesmí být záporný\")\n    if vek > 150:\n        raise ChybaVeku(\"vek\", \"Nereálná hodnota\")\n    return vek\n\ntry:\n    nastav_vek(-5)\nexcept ChybaVeku as e:\n    print(f\"Validace selhala: {e}\")\n    print(f\"Pole: {e.pole}\")</code></pre><h2>Kontextové manažery (with/as)</h2><p>Použij <code>with</code> pro automatický úklid prostředků:</p><pre><code class="language-python">class SpravaSouboru:\n    def __init__(self, nazev, mod):\n        self.nazev = nazev\n        self.mod = mod\n        self.soubor = None\n\n    def __enter__(self):\n        self.soubor = open(self.nazev, self.mod)\n        return self.soubor\n\n    def __exit__(self, typ, hodn, tb):\n        if self.soubor:\n            self.soubor.close()\n        return False\n\nwith SpravaSouboru(\"test.txt\", \"w\") as f:\n    f.write(\"Ahoj!\")</code></pre><h2>contextlib zkratka</h2><pre><code class="language-python">from contextlib import contextmanager\n\n@contextmanager\ndef casovac():\n    import time\n    start = time.time()\n    yield\n    uplynulo = time.time() - start\n    print(f\"Uplynulo: {uplynulo:.3f}s\")\n\nwith casovac():\n    soucet = sum(range(1000000))\n    print(f\"Součet: {soucet}\")</code></pre><div class="warning"><p><strong>Upozornění:</strong> Vždy používej kontextové manažery pro soubory, zámky a připojení k databázi.</p></div>'},csharp:{en:'<h1>Advanced Error Handling</h1><p>Go beyond basic try/catch. Create custom exceptions and use the <code>using</code> statement for resource management.</p><h2>Custom Exceptions</h2><p>Inherit from <code>Exception</code> to create your own:</p><pre><code class="language-csharp">class ValidationException : Exception {\n    public string Field { get; }\n    public ValidationException(string field, string msg)\n        : base($\"{field}: {msg}\") {\n        Field = field;\n    }\n}\n\nclass AgeException : ValidationException {\n    public AgeException(string msg)\n        : base(\"Age\", msg) {}\n}\n\nvoid SetAge(int age) {\n    if (age &lt; 0)\n        throw new AgeException(\"Cannot be negative\");\n    if (age > 150)\n        throw new AgeException(\"Unrealistic value\");\n}\n\ntry { SetAge(-5); }\ncatch (AgeException e) {\n    Console.WriteLine($\"Validation: {e.Message}\");\n    Console.WriteLine($\"Field: {e.Field}\");\n}</code></pre><h2>IDisposable and using</h2><p>Implement <code>IDisposable</code> for cleanup:</p><pre><code class="language-csharp">class ResourceHandler : IDisposable {\n    public string Name { get; }\n    public ResourceHandler(string name) {\n        Name = name;\n        Console.WriteLine($\"Opened: {name}\");\n    }\n    public void Dispose() {\n        Console.WriteLine($\"Closed: {Name}\");\n    }\n}\n\nusing (var res = new ResourceHandler(\"File\")) {\n    Console.WriteLine($\"Using: {res.Name}\");\n}\n// Dispose is called automatically</code></pre><h2>Exception Filters</h2><pre><code class="language-csharp">try {\n    int.Parse(\"abc\");\n} catch (FormatException e) when (e.Message.Contains(\"Input\")) {\n    Console.WriteLine(\"Specific format error\");\n} catch (FormatException) {\n    Console.WriteLine(\"General format error\");\n}</code></pre><div class="warning"><p><strong>Warning:</strong> Always use <code>using</code> for objects implementing IDisposable to prevent resource leaks.</p></div>',cz:'<h1>Pokročilé ošetření chyb</h1><p>Jdi dál za základní try/catch. Vytvoř si vlastní výjimky a používej <code>using</code> pro správu prostředků.</p><h2>Vlastní výjimky</h2><p>Zdědění z <code>Exception</code> pro vlastní třídu:</p><pre><code class="language-csharp">class ChybaValidace : Exception {\n    public string Pole { get; }\n    public ChybaValidace(string pole, string zprava)\n        : base($\"{pole}: {zprava}\") {\n        Pole = pole;\n    }\n}\n\nclass ChybaVeku : ChybaValidace {\n    public ChybaVeku(string zprava)\n        : base(\"Vek\", zprava) {}\n}\n\nvoid NastavVek(int vek) {\n    if (vek &lt; 0)\n        throw new ChybaVeku(\"Nesmí být záporný\");\n}\n\ntry { NastavVek(-5); }\ncatch (ChybaVeku e) {\n    Console.WriteLine($\"Validace: {e.Message}\");\n    Console.WriteLine($\"Pole: {e.Pole}\");\n}</code></pre><h2>IDisposable a using</h2><p>Implementuj <code>IDisposable</code> pro úklid:</p><pre><code class="language-csharp">class SpravaProstredku : IDisposable {\n    public string Nazev { get; }\n    public SpravaProstredku(string n) {\n        Nazev = n;\n        Console.WriteLine($\"Otevřeno: {n}\");\n    }\n    public void Dispose() {\n        Console.WriteLine($\"Zavřeno: {Nazev}\");\n    }\n}\n\nusing (var r = new SpravaProstredku(\"Soubor\")) {\n    Console.WriteLine($\"Používám: {r.Nazev}\");\n}</code></pre><h2>Filtry výjimek</h2><pre><code class="language-csharp">try {\n    int.Parse(\"abc\");\n} catch (FormatException e) when (e.Message.Contains(\"Input\")) {\n    Console.WriteLine(\"Specifická chyba formátu\");\n} catch (FormatException) {\n    Console.WriteLine(\"Obecná chyba formátu\");\n}</code></pre><div class="warning"><p><strong>Upozornění:</strong> Vždy používej <code>using</code> pro objekty implementující IDisposable.</p></div>'}}},
{id:34,difficulty:3,title:{python:{en:'Unit Testing Basics',cz:'Základy jednotkového testování'},csharp:{en:'Unit Testing Basics',cz:'Základy jednotkového testování'}},description:{python:{en:'Write tests with unittest module',cz:'Piš testy s modulem unittest'},csharp:{en:'Write tests with NUnit concepts',cz:'Piš testy s principy NUnit'}},content:{python:{en:'<h1>Unit Testing Basics</h1><p>Unit tests verify that individual pieces of code work correctly. Python includes the <code>unittest</code> module in its standard library.</p><h2>Your First Test</h2><pre><code class="language-python">import unittest\n\ndef add(a, b):\n    return a + b\n\ndef divide(a, b):\n    if b == 0:\n        raise ValueError(\"Cannot divide by zero\")\n    return a / b\n\nclass TestMath(unittest.TestCase):\n    def test_add(self):\n        self.assertEqual(add(2, 3), 5)\n        self.assertEqual(add(-1, 1), 0)\n\n    def test_divide(self):\n        self.assertAlmostEqual(divide(10, 3), 3.333, places=2)\n\n    def test_divide_by_zero(self):\n        with self.assertRaises(ValueError):\n            divide(10, 0)\n\nif __name__ == \"__main__\":\n    unittest.main()</code></pre><h2>Assert Methods</h2><p>Common assertions you can use:</p><pre><code class="language-python">class TestAssertions(unittest.TestCase):\n    def test_equality(self):\n        self.assertEqual(1 + 1, 2)\n        self.assertNotEqual(1, 2)\n\n    def test_truthiness(self):\n        self.assertTrue(10 > 5)\n        self.assertFalse(5 > 10)\n\n    def test_membership(self):\n        self.assertIn(\"a\", [\"a\", \"b\", \"c\"])\n        self.assertIsNone(None)\n        self.assertIsNotNone(42)</code></pre><h2>setUp and tearDown</h2><pre><code class="language-python">class TestStack(unittest.TestCase):\n    def setUp(self):\n        self.stack = []\n\n    def test_push(self):\n        self.stack.append(1)\n        self.assertEqual(len(self.stack), 1)\n\n    def test_pop(self):\n        self.stack.append(1)\n        val = self.stack.pop()\n        self.assertEqual(val, 1)\n        self.assertEqual(len(self.stack), 0)</code></pre>',cz:'<h1>Základy jednotkového testování</h1><p>Jednotkové testy ověřují, že jednotlivé části kódu fungují správně. Python obsahuje modul <code>unittest</code> ve standardní knihovně.</p><h2>Tvůj první test</h2><pre><code class="language-python">import unittest\n\ndef secti(a, b):\n    return a + b\n\ndef vydel(a, b):\n    if b == 0:\n        raise ValueError(\"Nelze dělit nulou\")\n    return a / b\n\nclass TestMatematika(unittest.TestCase):\n    def test_soucet(self):\n        self.assertEqual(secti(2, 3), 5)\n        self.assertEqual(secti(-1, 1), 0)\n\n    def test_deleni(self):\n        self.assertAlmostEqual(vydel(10, 3), 3.333, places=2)\n\n    def test_deleni_nulou(self):\n        with self.assertRaises(ValueError):\n            vydel(10, 0)\n\nif __name__ == \"__main__\":\n    unittest.main()</code></pre><h2>Metody assert</h2><pre><code class="language-python">class TestOvereni(unittest.TestCase):\n    def test_rovnost(self):\n        self.assertEqual(1 + 1, 2)\n        self.assertNotEqual(1, 2)\n\n    def test_pravdivost(self):\n        self.assertTrue(10 > 5)\n        self.assertFalse(5 > 10)\n\n    def test_prislusnost(self):\n        self.assertIn(\"a\", [\"a\", \"b\", \"c\"])\n        self.assertIsNone(None)</code></pre><h2>setUp a tearDown</h2><pre><code class="language-python">class TestZasobnik(unittest.TestCase):\n    def setUp(self):\n        self.zasobnik = []\n\n    def test_vlozeni(self):\n        self.zasobnik.append(1)\n        self.assertEqual(len(self.zasobnik), 1)\n\n    def test_odebrani(self):\n        self.zasobnik.append(1)\n        hodnota = self.zasobnik.pop()\n        self.assertEqual(hodnota, 1)</code></pre>'},csharp:{en:'<h1>Unit Testing Basics</h1><p>Unit tests verify that individual pieces of code work correctly. In C# the popular framework is <strong>NUnit</strong>.</p><h2>Your First Test</h2><pre><code class="language-csharp">using NUnit.Framework;\n\npublic class Calculator {\n    public int Add(int a, int b) { return a + b; }\n    public double Divide(int a, int b) {\n        if (b == 0)\n            throw new DivideByZeroException();\n        return (double)a / b;\n    }\n}\n\n[TestFixture]\npublic class CalculatorTests {\n    private Calculator _calc;\n\n    [SetUp]\n    public void Setup() {\n        _calc = new Calculator();\n    }\n\n    [Test]\n    public void Add_TwoNumbers_ReturnsSum() {\n        Assert.AreEqual(5, _calc.Add(2, 3));\n        Assert.AreEqual(0, _calc.Add(-1, 1));\n    }\n\n    [Test]\n    public void Divide_ByZero_ThrowsException() {\n        Assert.Throws&lt;DivideByZeroException&gt;(\n            () => _calc.Divide(10, 0));\n    }\n}</code></pre><h2>Assert Methods</h2><pre><code class="language-csharp">[Test]\npublic void TestAssertions() {\n    Assert.AreEqual(2, 1 + 1);\n    Assert.AreNotEqual(1, 2);\n    Assert.IsTrue(10 > 5);\n    Assert.IsFalse(5 > 10);\n    Assert.IsNull(null);\n    Assert.IsNotNull(42);\n    Assert.Contains(\"a\", new[] {\"a\", \"b\", \"c\"});\n}</code></pre><h2>Test Lifecycle</h2><pre><code class="language-csharp">[TestFixture]\npublic class StackTests {\n    private Stack&lt;int&gt; _stack;\n\n    [SetUp]\n    public void Setup() {\n        _stack = new Stack&lt;int&gt;();\n    }\n\n    [Test]\n    public void Push_AddsElement() {\n        _stack.Push(1);\n        Assert.AreEqual(1, _stack.Count);\n    }\n\n    [TearDown]\n    public void Cleanup() {\n        _stack.Clear();\n    }\n}</code></pre>',cz:'<h1>Základy jednotkového testování</h1><p>Jednotkové testy ověřují správnost kódu. V C# se používá framework <strong>NUnit</strong>.</p><h2>Tvůj první test</h2><pre><code class="language-csharp">using NUnit.Framework;\n\npublic class Kalkulacka {\n    public int Secti(int a, int b) { return a + b; }\n    public double Vydel(int a, int b) {\n        if (b == 0)\n            throw new DivideByZeroException();\n        return (double)a / b;\n    }\n}\n\n[TestFixture]\npublic class TestyKalkulacky {\n    private Kalkulacka _calc;\n\n    [SetUp]\n    public void Priprava() {\n        _calc = new Kalkulacka();\n    }\n\n    [Test]\n    public void Secti_DveCisla_VratiSoucet() {\n        Assert.AreEqual(5, _calc.Secti(2, 3));\n        Assert.AreEqual(0, _calc.Secti(-1, 1));\n    }\n\n    [Test]\n    public void Vydel_Nulou_VyhodiVyjimku() {\n        Assert.Throws&lt;DivideByZeroException&gt;(\n            () => _calc.Vydel(10, 0));\n    }\n}</code></pre><h2>Metody Assert</h2><pre><code class="language-csharp">[Test]\npublic void TestOvereni() {\n    Assert.AreEqual(2, 1 + 1);\n    Assert.AreNotEqual(1, 2);\n    Assert.IsTrue(10 > 5);\n    Assert.IsFalse(5 > 10);\n    Assert.IsNull(null);\n    Assert.IsNotNull(42);\n}</code></pre><h2>Životní cyklus testu</h2><pre><code class="language-csharp">[TestFixture]\npublic class TestyZasobniku {\n    private Stack&lt;int&gt; _zasobnik;\n\n    [SetUp]\n    public void Priprava() {\n        _zasobnik = new Stack&lt;int&gt;();\n    }\n\n    [Test]\n    public void Push_PridaPrvek() {\n        _zasobnik.Push(1);\n        Assert.AreEqual(1, _zasobnik.Count);\n    }\n\n    [TearDown]\n    public void Uklid() {\n        _zasobnik.Clear();\n    }\n}</code></pre>'}}},
{id:35,difficulty:3,title:{python:{en:'Clean Code Principles',cz:'Principy čistého kódu'},csharp:{en:'Clean Code Principles',cz:'Principy čistého kódu'}},description:{python:{en:'Write readable, maintainable Python code',cz:'Piš čitelný a udržovatelný kód v Pythonu'},csharp:{en:'Write readable, maintainable C# code',cz:'Piš čitelný a udržovatelný kód v C#'}},content:{python:{en:'<h1>Clean Code Principles</h1><p>Clean code is easy to read, understand, and maintain. Follow <strong>PEP 8</strong> and <strong>SOLID</strong> principles.</p><h2>PEP 8 Naming Conventions</h2><pre><code class="language-python"># Good - snake_case for functions and variables\ndef calculate_total_price(items):\n    total_price = 0\n    for item in items:\n        total_price += item[\"price\"]\n    return total_price\n\n# Bad - unclear names\ndef calc(x):\n    t = 0\n    for i in x:\n        t += i[\"price\"]\n    return t\n\n# Constants: UPPER_CASE\nMAX_RETRIES = 3\nDEFAULT_TIMEOUT = 30</code></pre><h2>Single Responsibility</h2><p>Each function should do one thing well:</p><pre><code class="language-python"># Bad - does too much\ndef process_user(data):\n    # validates, saves, sends email...\n    pass\n\n# Good - separated concerns\ndef validate_user(data):\n    if not data.get(\"name\"):\n        raise ValueError(\"Name required\")\n    return True\n\ndef save_user(data):\n    print(f\"Saved: {data[\"name\"]}\")\n\ndef notify_user(email):\n    print(f\"Email sent to: {email}\")</code></pre><h2>DRY Principle</h2><pre><code class="language-python"># Bad - repeated logic\ndef get_adult_users(users):\n    return [u for u in users if u[\"age\"] >= 18]\n\ndef get_adult_names(users):\n    return [u[\"name\"] for u in users if u[\"age\"] >= 18]\n\n# Good - reuse the filter\ndef get_adults(users):\n    return [u for u in users if u[\"age\"] >= 18]\n\ndef get_names(users):\n    return [u[\"name\"] for u in users]\n\n# Compose: get_names(get_adults(users))</code></pre>',cz:'<h1>Principy čistého kódu</h1><p>Čistý kód je snadno čitelný, pochopitelný a udržovatelný. Dodržuj <strong>PEP 8</strong> a principy <strong>SOLID</strong>.</p><h2>PEP 8 konvence pojmenování</h2><pre><code class="language-python"># Dobře - snake_case pro funkce a proměnné\ndef vypocitej_celkovou_cenu(polozky):\n    celkova_cena = 0\n    for polozka in polozky:\n        celkova_cena += polozka[\"cena\"]\n    return celkova_cena\n\n# Špatně - nejasné názvy\ndef vyp(x):\n    t = 0\n    for i in x:\n        t += i[\"cena\"]\n    return t\n\n# Konstanty: VELKA_PISMENA\nMAX_POKUSU = 3\nVYCHOZI_TIMEOUT = 30</code></pre><h2>Jediná odpovědnost</h2><p>Každá funkce by měla dělat jednu věc dobře:</p><pre><code class="language-python"># Špatně - dělá příliš mnoho\ndef zpracuj_uzivatele(data):\n    pass  # validuje, ukládá, posílá email...\n\n# Dobře - oddělené zodpovědnosti\ndef validuj_uzivatele(data):\n    if not data.get(\"jmeno\"):\n        raise ValueError(\"Jméno je povinné\")\n    return True\n\ndef uloz_uzivatele(data):\n    print(f\"Uloženo: {data[\"jmeno\"]}\")\n\ndef upozorni_uzivatele(email):\n    print(f\"Email odeslán na: {email}\")</code></pre><h2>Princip DRY</h2><pre><code class="language-python"># Dobře - znovupoužitelné funkce\ndef ziskej_dospele(uzivatele):\n    return [u for u in uzivatele if u[\"vek\"] >= 18]\n\ndef ziskej_jmena(uzivatele):\n    return [u[\"jmeno\"] for u in uzivatele]\n\n# Skládání: ziskej_jmena(ziskej_dospele(uzivatele))</code></pre>'},csharp:{en:'<h1>Clean Code Principles</h1><p>Clean code is easy to read, understand, and maintain. Follow C# conventions and <strong>SOLID</strong> principles.</p><h2>C# Naming Standards</h2><pre><code class="language-csharp">// PascalCase for classes, methods, properties\npublic class OrderProcessor {\n    public decimal TotalPrice { get; private set; }\n    private readonly int _maxRetries = 3;  // _camelCase for private fields\n\n    public decimal CalculateTotal(List&lt;decimal&gt; prices) {\n        decimal total = 0;  // camelCase for local vars\n        foreach (var price in prices)\n            total += price;\n        return total;\n    }\n}\n\n// Interface names start with I\npublic interface IPaymentService {\n    bool ProcessPayment(decimal amount);\n}</code></pre><h2>Single Responsibility</h2><pre><code class="language-csharp">// Bad - one class does everything\nclass UserManager {\n    void Validate() {}\n    void Save() {}\n    void SendEmail() {}\n}\n\n// Good - separate concerns\nclass UserValidator {\n    public bool Validate(string name) {\n        return !string.IsNullOrEmpty(name);\n    }\n}\nclass UserRepository {\n    public void Save(string name) {\n        Console.WriteLine($\"Saved: {name}\");\n    }\n}\nclass EmailService {\n    public void Send(string to) {\n        Console.WriteLine($\"Email to: {to}\");\n    }\n}</code></pre><h2>Open/Closed Principle</h2><pre><code class="language-csharp">// Open for extension, closed for modification\nabstract class Shape {\n    public abstract double Area();\n}\nclass Rectangle : Shape {\n    public double Width, Height;\n    public override double Area() => Width * Height;\n}\nclass Circle : Shape {\n    public double Radius;\n    public override double Area() => Math.PI * Radius * Radius;\n}\n\n// No need to modify existing code to add Triangle\nvoid PrintArea(Shape s) {\n    Console.WriteLine($\"Area: {s.Area()}\");\n}</code></pre>',cz:'<h1>Principy čistého kódu</h1><p>Čistý kód je snadno čitelný a udržovatelný. Dodržuj konvence C# a principy <strong>SOLID</strong>.</p><h2>Konvence pojmenování v C#</h2><pre><code class="language-csharp">// PascalCase pro třídy, metody, vlastnosti\npublic class ZpracovatelObjednavek {\n    public decimal CelkovaCena { get; private set; }\n    private readonly int _maxPokusu = 3;  // _camelCase pro privátní\n\n    public decimal VypocitejCelkem(List&lt;decimal&gt; ceny) {\n        decimal celkem = 0;\n        foreach (var cena in ceny)\n            celkem += cena;\n        return celkem;\n    }\n}\n\n// Rozhraní začínají na I\npublic interface ISluzbaPlaceni {\n    bool ZpracujPlatbu(decimal castka);\n}</code></pre><h2>Jediná odpovědnost</h2><pre><code class="language-csharp">// Špatně - jedna třída dělá vše\nclass SpravceUzivatelu {\n    void Validuj() {}\n    void Uloz() {}\n    void PosliEmail() {}\n}\n\n// Dobře - oddělené odpovědnosti\nclass ValidatorUzivatele {\n    public bool Validuj(string jmeno) {\n        return !string.IsNullOrEmpty(jmeno);\n    }\n}\nclass UlozisteUzivatelu {\n    public void Uloz(string jmeno) {\n        Console.WriteLine($\"Uloženo: {jmeno}\");\n    }\n}</code></pre><h2>Princip otevřenosti/uzavřenosti</h2><pre><code class="language-csharp">abstract class Tvar {\n    public abstract double Obsah();\n}\nclass Obdelnik : Tvar {\n    public double Sirka, Vyska;\n    public override double Obsah() => Sirka * Vyska;\n}\nclass Kruh : Tvar {\n    public double Polomer;\n    public override double Obsah() => Math.PI * Polomer * Polomer;\n}\n\nvoid VypisObsah(Tvar t) {\n    Console.WriteLine($\"Obsah: {t.Obsah()}\");\n}</code></pre>'}}},
{id:36,difficulty:4,title:{python:{en:'Linked Lists',cz:'Spojové seznamy'},csharp:{en:'Linked Lists',cz:'Spojové seznamy'}},description:{python:{en:'Implement a linked list data structure',cz:'Implementuj datovou strukturu spojového seznamu'},csharp:{en:'Implement a linked list data structure',cz:'Implementuj datovou strukturu spojového seznamu'}},content:{python:{en:'<h1>Linked Lists</h1><p>A linked list is a data structure where each element (node) points to the next one. Unlike arrays, linked lists do not store elements contiguously in memory.</p><h2>Node Class</h2><pre><code class="language-python">class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\n    def __repr__(self):\n        return f\"Node({self.data})\"</code></pre><h2>LinkedList Implementation</h2><pre><code class="language-python">class LinkedList:\n    def __init__(self):\n        self.head = None\n\n    def append(self, data):\n        new_node = Node(data)\n        if self.head is None:\n            self.head = new_node\n            return\n        current = self.head\n        while current.next:\n            current = current.next\n        current.next = new_node\n\n    def prepend(self, data):\n        new_node = Node(data)\n        new_node.next = self.head\n        self.head = new_node\n\n    def delete(self, data):\n        if self.head and self.head.data == data:\n            self.head = self.head.next\n            return\n        current = self.head\n        while current and current.next:\n            if current.next.data == data:\n                current.next = current.next.next\n                return\n            current = current.next\n\n    def display(self):\n        elements = []\n        current = self.head\n        while current:\n            elements.append(str(current.data))\n            current = current.next\n        print(\" -> \".join(elements))\n\nll = LinkedList()\nll.append(1)\nll.append(2)\nll.append(3)\nll.prepend(0)\nll.display()   # 0 -> 1 -> 2 -> 3\nll.delete(2)\nll.display()   # 0 -> 1 -> 3</code></pre><h2>Search</h2><pre><code class="language-python">def search(self, data):\n    current = self.head\n    index = 0\n    while current:\n        if current.data == data:\n            return index\n        current = current.next\n        index += 1\n    return -1</code></pre>',cz:'<h1>Spojové seznamy</h1><p>Spojový seznam je datová struktura, kde každý prvek (uzel) ukazuje na další. Na rozdíl od polí se prvky neukládají souvisle v paměti.</p><h2>Třída Node</h2><pre><code class="language-python">class Uzel:\n    def __init__(self, data):\n        self.data = data\n        self.dalsi = None\n\n    def __repr__(self):\n        return f\"Uzel({self.data})\"</code></pre><h2>Implementace spojového seznamu</h2><pre><code class="language-python">class SpojovySeznam:\n    def __init__(self):\n        self.hlava = None\n\n    def pridej_na_konec(self, data):\n        novy = Uzel(data)\n        if self.hlava is None:\n            self.hlava = novy\n            return\n        aktualni = self.hlava\n        while aktualni.dalsi:\n            aktualni = aktualni.dalsi\n        aktualni.dalsi = novy\n\n    def pridej_na_zacatek(self, data):\n        novy = Uzel(data)\n        novy.dalsi = self.hlava\n        self.hlava = novy\n\n    def smaz(self, data):\n        if self.hlava and self.hlava.data == data:\n            self.hlava = self.hlava.dalsi\n            return\n        aktualni = self.hlava\n        while aktualni and aktualni.dalsi:\n            if aktualni.dalsi.data == data:\n                aktualni.dalsi = aktualni.dalsi.dalsi\n                return\n            aktualni = aktualni.dalsi\n\n    def zobraz(self):\n        prvky = []\n        aktualni = self.hlava\n        while aktualni:\n            prvky.append(str(aktualni.data))\n            aktualni = aktualni.dalsi\n        print(\" -> \".join(prvky))\n\nss = SpojovySeznam()\nss.pridej_na_konec(1)\nss.pridej_na_konec(2)\nss.pridej_na_zacatek(0)\nss.zobraz()   # 0 -> 1 -> 2\nss.smaz(1)\nss.zobraz()   # 0 -> 2</code></pre><h2>Vyhledávání</h2><pre><code class="language-python">def hledej(self, data):\n    aktualni = self.hlava\n    index = 0\n    while aktualni:\n        if aktualni.data == data:\n            return index\n        aktualni = aktualni.dalsi\n        index += 1\n    return -1</code></pre>'},csharp:{en:'<h1>Linked Lists</h1><p>A linked list is a data structure where each element (node) points to the next one. Unlike arrays, elements are not stored contiguously in memory.</p><h2>Node Class</h2><pre><code class="language-csharp">class Node {\n    public int Data;\n    public Node Next;\n    public Node(int data) {\n        Data = data;\n        Next = null;\n    }\n    public override string ToString() {\n        return $\"Node({Data})\";\n    }\n}</code></pre><h2>LinkedList Implementation</h2><pre><code class="language-csharp">class MyLinkedList {\n    public Node Head;\n\n    public void Append(int data) {\n        var newNode = new Node(data);\n        if (Head == null) { Head = newNode; return; }\n        var current = Head;\n        while (current.Next != null)\n            current = current.Next;\n        current.Next = newNode;\n    }\n\n    public void Prepend(int data) {\n        var newNode = new Node(data);\n        newNode.Next = Head;\n        Head = newNode;\n    }\n\n    public void Delete(int data) {\n        if (Head != null &amp;&amp; Head.Data == data) {\n            Head = Head.Next;\n            return;\n        }\n        var current = Head;\n        while (current?.Next != null) {\n            if (current.Next.Data == data) {\n                current.Next = current.Next.Next;\n                return;\n            }\n            current = current.Next;\n        }\n    }\n\n    public void Display() {\n        var current = Head;\n        while (current != null) {\n            Console.Write(current.Data + \" -> \");\n            current = current.Next;\n        }\n        Console.WriteLine(\"null\");\n    }\n}\n\nvar ll = new MyLinkedList();\nll.Append(1); ll.Append(2); ll.Append(3);\nll.Prepend(0);\nll.Display();   // 0 -> 1 -> 2 -> 3 -> null\nll.Delete(2);\nll.Display();   // 0 -> 1 -> 3 -> null</code></pre><h2>Search</h2><pre><code class="language-csharp">public int Search(int data) {\n    var current = Head;\n    int index = 0;\n    while (current != null) {\n        if (current.Data == data) return index;\n        current = current.Next;\n        index++;\n    }\n    return -1;\n}</code></pre>',cz:'<h1>Spojové seznamy</h1><p>Spojový seznam je datová struktura, kde každý prvek (uzel) ukazuje na další. Prvky nejsou uloženy souvisle v paměti.</p><h2>Třída Node</h2><pre><code class="language-csharp">class Uzel {\n    public int Data;\n    public Uzel Dalsi;\n    public Uzel(int data) {\n        Data = data;\n        Dalsi = null;\n    }\n}</code></pre><h2>Implementace spojového seznamu</h2><pre><code class="language-csharp">class SpojovySeznam {\n    public Uzel Hlava;\n\n    public void PridejNaKonec(int data) {\n        var novy = new Uzel(data);\n        if (Hlava == null) { Hlava = novy; return; }\n        var akt = Hlava;\n        while (akt.Dalsi != null)\n            akt = akt.Dalsi;\n        akt.Dalsi = novy;\n    }\n\n    public void PridejNaZacatek(int data) {\n        var novy = new Uzel(data);\n        novy.Dalsi = Hlava;\n        Hlava = novy;\n    }\n\n    public void Smaz(int data) {\n        if (Hlava != null &amp;&amp; Hlava.Data == data) {\n            Hlava = Hlava.Dalsi;\n            return;\n        }\n        var akt = Hlava;\n        while (akt?.Dalsi != null) {\n            if (akt.Dalsi.Data == data) {\n                akt.Dalsi = akt.Dalsi.Dalsi;\n                return;\n            }\n            akt = akt.Dalsi;\n        }\n    }\n\n    public void Zobraz() {\n        var akt = Hlava;\n        while (akt != null) {\n            Console.Write(akt.Data + \" -> \");\n            akt = akt.Dalsi;\n        }\n        Console.WriteLine(\"null\");\n    }\n}\n\nvar ss = new SpojovySeznam();\nss.PridejNaKonec(1); ss.PridejNaKonec(2);\nss.PridejNaZacatek(0);\nss.Zobraz();   // 0 -> 1 -> 2 -> null</code></pre><h2>Vyhledávání</h2><pre><code class="language-csharp">public int Hledej(int data) {\n    var akt = Hlava;\n    int index = 0;\n    while (akt != null) {\n        if (akt.Data == data) return index;\n        akt = akt.Dalsi;\n        index++;\n    }\n    return -1;\n}</code></pre>'}}},
{id:37,difficulty:4,title:{python:{en:'Binary Trees',cz:'Binární stromy'},csharp:{en:'Binary Trees',cz:'Binární stromy'}},description:{python:{en:'Implement a binary search tree',cz:'Implementuj binární vyhledávací strom'},csharp:{en:'Implement a binary search tree',cz:'Implementuj binární vyhledávací strom'}},content:{python:{en:'<h1>Binary Trees</h1><p>A binary search tree (BST) is a tree where each node has at most two children. Left children are smaller, right children are larger.</p><h2>BST Implementation</h2><pre><code class="language-python">class TreeNode:\n    def __init__(self, value):\n        self.value = value\n        self.left = None\n        self.right = None\n\nclass BST:\n    def __init__(self):\n        self.root = None\n\n    def insert(self, value):\n        if self.root is None:\n            self.root = TreeNode(value)\n        else:\n            self._insert(self.root, value)\n\n    def _insert(self, node, value):\n        if value &lt; node.value:\n            if node.left is None:\n                node.left = TreeNode(value)\n            else:\n                self._insert(node.left, value)\n        else:\n            if node.right is None:\n                node.right = TreeNode(value)\n            else:\n                self._insert(node.right, value)\n\ntree = BST()\nfor v in [5, 3, 7, 1, 4, 6, 8]:\n    tree.insert(v)</code></pre><h2>Inorder Traversal</h2><p>Inorder traversal visits left, root, right and returns sorted order:</p><pre><code class="language-python">def inorder(self, node=None, result=None):\n    if result is None:\n        result = []\n        node = self.root\n    if node:\n        self.inorder(node.left, result)\n        result.append(node.value)\n        self.inorder(node.right, result)\n    return result\n\nprint(tree.inorder())  # [1, 3, 4, 5, 6, 7, 8]</code></pre><h2>Search</h2><pre><code class="language-python">def search(self, value):\n    return self._search(self.root, value)\n\ndef _search(self, node, value):\n    if node is None:\n        return False\n    if value == node.value:\n        return True\n    elif value &lt; node.value:\n        return self._search(node.left, value)\n    else:\n        return self._search(node.right, value)\n\nprint(tree.search(4))   # True\nprint(tree.search(10))  # False</code></pre>',cz:'<h1>Binární stromy</h1><p>Binární vyhledávací strom (BST) je strom, kde každý uzel má nejvýše dva potomky. Levé jsou menší, pravé větší.</p><h2>Implementace BST</h2><pre><code class="language-python">class UzelStromu:\n    def __init__(self, hodnota):\n        self.hodnota = hodnota\n        self.levy = None\n        self.pravy = None\n\nclass BVS:\n    def __init__(self):\n        self.koren = None\n\n    def vloz(self, hodnota):\n        if self.koren is None:\n            self.koren = UzelStromu(hodnota)\n        else:\n            self._vloz(self.koren, hodnota)\n\n    def _vloz(self, uzel, hodnota):\n        if hodnota &lt; uzel.hodnota:\n            if uzel.levy is None:\n                uzel.levy = UzelStromu(hodnota)\n            else:\n                self._vloz(uzel.levy, hodnota)\n        else:\n            if uzel.pravy is None:\n                uzel.pravy = UzelStromu(hodnota)\n            else:\n                self._vloz(uzel.pravy, hodnota)\n\nstrom = BVS()\nfor h in [5, 3, 7, 1, 4, 6, 8]:\n    strom.vloz(h)</code></pre><h2>Průchod inorder</h2><p>Inorder navštíví levý, kořen, pravý a vrátí seřazené pořadí:</p><pre><code class="language-python">def inorder(self, uzel=None, vysledek=None):\n    if vysledek is None:\n        vysledek = []\n        uzel = self.koren\n    if uzel:\n        self.inorder(uzel.levy, vysledek)\n        vysledek.append(uzel.hodnota)\n        self.inorder(uzel.pravy, vysledek)\n    return vysledek\n\nprint(strom.inorder())  # [1, 3, 4, 5, 6, 7, 8]</code></pre><h2>Vyhledávání</h2><pre><code class="language-python">def hledej(self, hodnota):\n    return self._hledej(self.koren, hodnota)\n\ndef _hledej(self, uzel, hodnota):\n    if uzel is None:\n        return False\n    if hodnota == uzel.hodnota:\n        return True\n    elif hodnota &lt; uzel.hodnota:\n        return self._hledej(uzel.levy, hodnota)\n    else:\n        return self._hledej(uzel.pravy, hodnota)\n\nprint(strom.hledej(4))   # True\nprint(strom.hledej(10))  # False</code></pre>'},csharp:{en:'<h1>Binary Trees</h1><p>A binary search tree (BST) is a tree where each node has at most two children. Left children are smaller, right children are larger.</p><h2>BST Implementation</h2><pre><code class="language-csharp">class TreeNode {\n    public int Value;\n    public TreeNode Left, Right;\n    public TreeNode(int val) {\n        Value = val;\n        Left = Right = null;\n    }\n}\n\nclass BST {\n    public TreeNode Root;\n\n    public void Insert(int value) {\n        Root = InsertRec(Root, value);\n    }\n\n    private TreeNode InsertRec(TreeNode node, int value) {\n        if (node == null) return new TreeNode(value);\n        if (value &lt; node.Value)\n            node.Left = InsertRec(node.Left, value);\n        else\n            node.Right = InsertRec(node.Right, value);\n        return node;\n    }\n}\n\nvar tree = new BST();\nforeach (int v in new[] {5, 3, 7, 1, 4, 6, 8})\n    tree.Insert(v);</code></pre><h2>Inorder Traversal</h2><p>Returns elements in sorted order:</p><pre><code class="language-csharp">void Inorder(TreeNode node) {\n    if (node == null) return;\n    Inorder(node.Left);\n    Console.Write(node.Value + \" \");\n    Inorder(node.Right);\n}\n\nInorder(tree.Root);  // 1 3 4 5 6 7 8</code></pre><h2>Search</h2><pre><code class="language-csharp">bool Search(TreeNode node, int value) {\n    if (node == null) return false;\n    if (value == node.Value) return true;\n    if (value &lt; node.Value)\n        return Search(node.Left, value);\n    return Search(node.Right, value);\n}\n\nConsole.WriteLine(Search(tree.Root, 4));   // True\nConsole.WriteLine(Search(tree.Root, 10));  // False</code></pre>',cz:'<h1>Binární stromy</h1><p>Binární vyhledávací strom (BST) je strom, kde každý uzel má nejvýše dva potomky. Levé jsou menší, pravé větší.</p><h2>Implementace BST</h2><pre><code class="language-csharp">class UzelStromu {\n    public int Hodnota;\n    public UzelStromu Levy, Pravy;\n    public UzelStromu(int h) {\n        Hodnota = h;\n        Levy = Pravy = null;\n    }\n}\n\nclass BVS {\n    public UzelStromu Koren;\n\n    public void Vloz(int hodnota) {\n        Koren = VlozRek(Koren, hodnota);\n    }\n\n    private UzelStromu VlozRek(UzelStromu uzel, int hodnota) {\n        if (uzel == null) return new UzelStromu(hodnota);\n        if (hodnota &lt; uzel.Hodnota)\n            uzel.Levy = VlozRek(uzel.Levy, hodnota);\n        else\n            uzel.Pravy = VlozRek(uzel.Pravy, hodnota);\n        return uzel;\n    }\n}\n\nvar strom = new BVS();\nforeach (int h in new[] {5, 3, 7, 1, 4, 6, 8})\n    strom.Vloz(h);</code></pre><h2>Průchod inorder</h2><p>Vrátí prvky v seřazeném pořadí:</p><pre><code class="language-csharp">void Inorder(UzelStromu uzel) {\n    if (uzel == null) return;\n    Inorder(uzel.Levy);\n    Console.Write(uzel.Hodnota + \" \");\n    Inorder(uzel.Pravy);\n}\n\nInorder(strom.Koren);  // 1 3 4 5 6 7 8</code></pre><h2>Vyhledávání</h2><pre><code class="language-csharp">bool Hledej(UzelStromu uzel, int hodnota) {\n    if (uzel == null) return false;\n    if (hodnota == uzel.Hodnota) return true;\n    if (hodnota &lt; uzel.Hodnota)\n        return Hledej(uzel.Levy, hodnota);\n    return Hledej(uzel.Pravy, hodnota);\n}\n\nConsole.WriteLine(Hledej(strom.Koren, 4));   // True\nConsole.WriteLine(Hledej(strom.Koren, 10));  // False</code></pre>'}}},
{id:38,difficulty:4,title:{python:{en:'Hash Tables',cz:'Hashovací tabulky'},csharp:{en:'Hash Tables',cz:'Hashovací tabulky'}},description:{python:{en:'Build a custom hash map with chaining',cz:'Postav vlastní hashovací mapu s řetězením'},csharp:{en:'Build a custom hash map with chaining',cz:'Postav vlastní hashovací mapu s řetězením'}},content:{python:{en:'<h1>Hash Tables</h1><p>A hash table maps keys to values using a hash function. Collisions are handled by <strong>chaining</strong> (each bucket is a list).</p><h2>Custom HashMap</h2><pre><code class="language-python">class HashMap:\n    def __init__(self, size=10):\n        self.size = size\n        self.buckets = [[] for _ in range(size)]\n\n    def _hash(self, key):\n        return hash(key) % self.size\n\n    def put(self, key, value):\n        idx = self._hash(key)\n        for i, (k, v) in enumerate(self.buckets[idx]):\n            if k == key:\n                self.buckets[idx][i] = (key, value)\n                return\n        self.buckets[idx].append((key, value))\n\n    def get(self, key):\n        idx = self._hash(key)\n        for k, v in self.buckets[idx]:\n            if k == key:\n                return v\n        raise KeyError(key)\n\n    def delete(self, key):\n        idx = self._hash(key)\n        for i, (k, v) in enumerate(self.buckets[idx]):\n            if k == key:\n                del self.buckets[idx][i]\n                return\n        raise KeyError(key)\n\nhm = HashMap()\nhm.put(\"name\", \"Alice\")\nhm.put(\"age\", 30)\nprint(hm.get(\"name\"))  # Alice\nprint(hm.get(\"age\"))   # 30</code></pre><h2>The __hash__ Method</h2><p>Make your objects hashable:</p><pre><code class="language-python">class Point:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n\n    def __hash__(self):\n        return hash((self.x, self.y))\n\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\npoints = {}\np = Point(1, 2)\npoints[p] = \"origin\"\nprint(points[Point(1, 2)])  # origin</code></pre><h2>Counting with Dicts</h2><pre><code class="language-python">def count_chars(text):\n    freq = {}\n    for ch in text:\n        freq[ch] = freq.get(ch, 0) + 1\n    return freq\n\nprint(count_chars(\"hello\"))\n# {\"h\": 1, \"e\": 1, \"l\": 2, \"o\": 1}</code></pre>',cz:'<h1>Hashovací tabulky</h1><p>Hashovací tabulka mapuje klíče na hodnoty pomocí hashovací funkce. Kolize se řeší <strong>řetězením</strong> (každý koš je seznam).</p><h2>Vlastní HashMap</h2><pre><code class="language-python">class HashMapa:\n    def __init__(self, velikost=10):\n        self.velikost = velikost\n        self.kose = [[] for _ in range(velikost)]\n\n    def _hash(self, klic):\n        return hash(klic) % self.velikost\n\n    def vloz(self, klic, hodnota):\n        idx = self._hash(klic)\n        for i, (k, h) in enumerate(self.kose[idx]):\n            if k == klic:\n                self.kose[idx][i] = (klic, hodnota)\n                return\n        self.kose[idx].append((klic, hodnota))\n\n    def ziskej(self, klic):\n        idx = self._hash(klic)\n        for k, h in self.kose[idx]:\n            if k == klic:\n                return h\n        raise KeyError(klic)\n\n    def smaz(self, klic):\n        idx = self._hash(klic)\n        for i, (k, h) in enumerate(self.kose[idx]):\n            if k == klic:\n                del self.kose[idx][i]\n                return\n        raise KeyError(klic)\n\nhm = HashMapa()\nhm.vloz(\"jmeno\", \"Alice\")\nhm.vloz(\"vek\", 30)\nprint(hm.ziskej(\"jmeno\"))  # Alice</code></pre><h2>Metoda __hash__</h2><pre><code class="language-python">class Bod:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n\n    def __hash__(self):\n        return hash((self.x, self.y))\n\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\nbody = {}\np = Bod(1, 2)\nbody[p] = \"počátek\"\nprint(body[Bod(1, 2)])  # počátek</code></pre><h2>Počítání znaků</h2><pre><code class="language-python">def pocitej_znaky(text):\n    frekvence = {}\n    for z in text:\n        frekvence[z] = frekvence.get(z, 0) + 1\n    return frekvence\n\nprint(pocitej_znaky(\"ahoj\"))</code></pre>'},csharp:{en:'<h1>Hash Tables</h1><p>A hash table maps keys to values using a hash function. Collisions are handled by <strong>chaining</strong> (each bucket is a list).</p><h2>Custom HashMap</h2><pre><code class="language-csharp">class MyHashMap {\n    private List&lt;KeyValuePair&lt;string,int&gt;&gt;[] buckets;\n    private int size;\n\n    public MyHashMap(int size = 10) {\n        this.size = size;\n        buckets = new List&lt;KeyValuePair&lt;string,int&gt;&gt;[size];\n        for (int i = 0; i &lt; size; i++)\n            buckets[i] = new List&lt;KeyValuePair&lt;string,int&gt;&gt;();\n    }\n\n    private int Hash(string key) {\n        return Math.Abs(key.GetHashCode()) % size;\n    }\n\n    public void Put(string key, int value) {\n        int idx = Hash(key);\n        for (int i = 0; i &lt; buckets[idx].Count; i++) {\n            if (buckets[idx][i].Key == key) {\n                buckets[idx][i] = new KeyValuePair&lt;string,int&gt;(key, value);\n                return;\n            }\n        }\n        buckets[idx].Add(new KeyValuePair&lt;string,int&gt;(key, value));\n    }\n\n    public int Get(string key) {\n        int idx = Hash(key);\n        foreach (var kv in buckets[idx])\n            if (kv.Key == key) return kv.Value;\n        throw new KeyNotFoundException(key);\n    }\n}\n\nvar hm = new MyHashMap();\nhm.Put(\"age\", 30);\nhm.Put(\"score\", 95);\nConsole.WriteLine(hm.Get(\"age\"));    // 30\nConsole.WriteLine(hm.Get(\"score\"));  // 95</code></pre><h2>GetHashCode Override</h2><pre><code class="language-csharp">class Point {\n    public int X, Y;\n    public Point(int x, int y) { X = x; Y = y; }\n\n    public override int GetHashCode() {\n        return HashCode.Combine(X, Y);\n    }\n    public override bool Equals(object obj) {\n        if (obj is Point p)\n            return X == p.X &amp;&amp; Y == p.Y;\n        return false;\n    }\n}\n\nvar dict = new Dictionary&lt;Point, string&gt;();\ndict[new Point(1, 2)] = \"A\";\nConsole.WriteLine(dict[new Point(1, 2)]);  // A</code></pre><h2>Counting Characters</h2><pre><code class="language-csharp">var freq = new Dictionary&lt;char, int&gt;();\nforeach (char c in \"hello\") {\n    if (freq.ContainsKey(c)) freq[c]++;\n    else freq[c] = 1;\n}\nforeach (var kv in freq)\n    Console.WriteLine($\"{kv.Key}: {kv.Value}\");</code></pre>',cz:'<h1>Hashovací tabulky</h1><p>Hashovací tabulka mapuje klíče na hodnoty pomocí hashovací funkce. Kolize se řeší <strong>řetězením</strong>.</p><h2>Vlastní HashMap</h2><pre><code class="language-csharp">class MojeHashMapa {\n    private List&lt;KeyValuePair&lt;string,int&gt;&gt;[] kose;\n    private int velikost;\n\n    public MojeHashMapa(int vel = 10) {\n        velikost = vel;\n        kose = new List&lt;KeyValuePair&lt;string,int&gt;&gt;[vel];\n        for (int i = 0; i &lt; vel; i++)\n            kose[i] = new List&lt;KeyValuePair&lt;string,int&gt;&gt;();\n    }\n\n    private int Hash(string klic) {\n        return Math.Abs(klic.GetHashCode()) % velikost;\n    }\n\n    public void Vloz(string klic, int hodnota) {\n        int idx = Hash(klic);\n        for (int i = 0; i &lt; kose[idx].Count; i++) {\n            if (kose[idx][i].Key == klic) {\n                kose[idx][i] = new KeyValuePair&lt;string,int&gt;(klic, hodnota);\n                return;\n            }\n        }\n        kose[idx].Add(new KeyValuePair&lt;string,int&gt;(klic, hodnota));\n    }\n\n    public int Ziskej(string klic) {\n        int idx = Hash(klic);\n        foreach (var kv in kose[idx])\n            if (kv.Key == klic) return kv.Value;\n        throw new KeyNotFoundException(klic);\n    }\n}\n\nvar hm = new MojeHashMapa();\nhm.Vloz(\"vek\", 30);\nConsole.WriteLine(hm.Ziskej(\"vek\"));  // 30</code></pre><h2>GetHashCode</h2><pre><code class="language-csharp">class Bod {\n    public int X, Y;\n    public Bod(int x, int y) { X = x; Y = y; }\n\n    public override int GetHashCode() {\n        return HashCode.Combine(X, Y);\n    }\n    public override bool Equals(object obj) {\n        if (obj is Bod b)\n            return X == b.X &amp;&amp; Y == b.Y;\n        return false;\n    }\n}\n\nvar slovnik = new Dictionary&lt;Bod, string&gt;();\nslovnik[new Bod(1, 2)] = \"A\";\nConsole.WriteLine(slovnik[new Bod(1, 2)]);</code></pre><h2>Počítání znaků</h2><pre><code class="language-csharp">var freq = new Dictionary&lt;char, int&gt;();\nforeach (char z in \"ahoj\") {\n    if (freq.ContainsKey(z)) freq[z]++;\n    else freq[z] = 1;\n}\nforeach (var kv in freq)\n    Console.WriteLine($\"{kv.Key}: {kv.Value}\");</code></pre>'}}},
{id:39,difficulty:4,title:{python:{en:'Graphs',cz:'Grafy'},csharp:{en:'Graphs',cz:'Grafy'}},description:{python:{en:'Represent graphs and traverse with BFS and DFS',cz:'Reprezentuj grafy a procházej pomocí BFS a DFS'},csharp:{en:'Represent graphs and traverse with BFS and DFS',cz:'Reprezentuj grafy a procházej pomocí BFS a DFS'}},content:{python:{en:'<h1>Graphs</h1><p>A graph consists of vertices (nodes) and edges connecting them. We use an <strong>adjacency list</strong> to represent graphs efficiently.</p><h2>Graph with Adjacency List</h2><pre><code class="language-python">class Graph:\n    def __init__(self):\n        self.adj = {}\n\n    def add_vertex(self, v):\n        if v not in self.adj:\n            self.adj[v] = []\n\n    def add_edge(self, u, v):\n        self.add_vertex(u)\n        self.add_vertex(v)\n        self.adj[u].append(v)\n        self.adj[v].append(u)  # undirected\n\ng = Graph()\ng.add_edge(\"A\", \"B\")\ng.add_edge(\"A\", \"C\")\ng.add_edge(\"B\", \"D\")\ng.add_edge(\"C\", \"D\")\ng.add_edge(\"D\", \"E\")</code></pre><h2>BFS (Breadth-First Search)</h2><p>BFS explores neighbors level by level using a queue:</p><pre><code class="language-python">from collections import deque\n\ndef bfs(graph, start):\n    visited = set()\n    queue = deque([start])\n    visited.add(start)\n    result = []\n\n    while queue:\n        vertex = queue.popleft()\n        result.append(vertex)\n        for neighbor in graph.adj[vertex]:\n            if neighbor not in visited:\n                visited.add(neighbor)\n                queue.append(neighbor)\n    return result\n\nprint(bfs(g, \"A\"))  # [\"A\", \"B\", \"C\", \"D\", \"E\"]</code></pre><h2>DFS (Depth-First Search)</h2><p>DFS explores as deep as possible before backtracking:</p><pre><code class="language-python">def dfs(graph, start, visited=None):\n    if visited is None:\n        visited = set()\n    visited.add(start)\n    result = [start]\n    for neighbor in graph.adj[start]:\n        if neighbor not in visited:\n            result.extend(dfs(graph, neighbor, visited))\n    return result\n\nprint(dfs(g, \"A\"))  # [\"A\", \"B\", \"D\", \"C\", \"E\"]</code></pre>',cz:'<h1>Grafy</h1><p>Graf se skládá z vrcholů (uzlů) a hran, které je spojují. Používáme <strong>seznam sousedů</strong> pro efektivní reprezentaci.</p><h2>Graf se seznamem sousedů</h2><pre><code class="language-python">class Graf:\n    def __init__(self):\n        self.adj = {}\n\n    def pridej_vrchol(self, v):\n        if v not in self.adj:\n            self.adj[v] = []\n\n    def pridej_hranu(self, u, v):\n        self.pridej_vrchol(u)\n        self.pridej_vrchol(v)\n        self.adj[u].append(v)\n        self.adj[v].append(u)  # neorientovaný\n\ng = Graf()\ng.pridej_hranu(\"A\", \"B\")\ng.pridej_hranu(\"A\", \"C\")\ng.pridej_hranu(\"B\", \"D\")\ng.pridej_hranu(\"C\", \"D\")\ng.pridej_hranu(\"D\", \"E\")</code></pre><h2>BFS (prohledávání do šířky)</h2><p>BFS prozkoumá sousedy po úrovních pomocí fronty:</p><pre><code class="language-python">from collections import deque\n\ndef bfs(graf, start):\n    navstivene = set()\n    fronta = deque([start])\n    navstivene.add(start)\n    vysledek = []\n\n    while fronta:\n        vrchol = fronta.popleft()\n        vysledek.append(vrchol)\n        for soused in graf.adj[vrchol]:\n            if soused not in navstivene:\n                navstivene.add(soused)\n                fronta.append(soused)\n    return vysledek\n\nprint(bfs(g, \"A\"))</code></pre><h2>DFS (prohledávání do hloubky)</h2><p>DFS prozkoumá cestu co nejhlouběji před návratem:</p><pre><code class="language-python">def dfs(graf, start, navstivene=None):\n    if navstivene is None:\n        navstivene = set()\n    navstivene.add(start)\n    vysledek = [start]\n    for soused in graf.adj[start]:\n        if soused not in navstivene:\n            vysledek.extend(dfs(graf, soused, navstivene))\n    return vysledek\n\nprint(dfs(g, \"A\"))</code></pre>'},csharp:{en:'<h1>Graphs</h1><p>A graph consists of vertices and edges. We use an <strong>adjacency list</strong> for efficient representation.</p><h2>Graph with Adjacency List</h2><pre><code class="language-csharp">class Graph {\n    private Dictionary&lt;string, List&lt;string&gt;&gt; adj\n        = new Dictionary&lt;string, List&lt;string&gt;&gt;();\n\n    public void AddEdge(string u, string v) {\n        if (!adj.ContainsKey(u)) adj[u] = new List&lt;string&gt;();\n        if (!adj.ContainsKey(v)) adj[v] = new List&lt;string&gt;();\n        adj[u].Add(v);\n        adj[v].Add(u);  // undirected\n    }\n\n    public List&lt;string&gt; GetNeighbors(string v) {\n        return adj.ContainsKey(v) ? adj[v] : new List&lt;string&gt;();\n    }\n\n    public IEnumerable&lt;string&gt; Vertices => adj.Keys;\n}\n\nvar g = new Graph();\ng.AddEdge(\"A\", \"B\");\ng.AddEdge(\"A\", \"C\");\ng.AddEdge(\"B\", \"D\");\ng.AddEdge(\"C\", \"D\");\ng.AddEdge(\"D\", \"E\");</code></pre><h2>BFS (Breadth-First Search)</h2><pre><code class="language-csharp">List&lt;string&gt; BFS(Graph graph, string start) {\n    var visited = new HashSet&lt;string&gt;();\n    var queue = new Queue&lt;string&gt;();\n    var result = new List&lt;string&gt;();\n    queue.Enqueue(start);\n    visited.Add(start);\n\n    while (queue.Count > 0) {\n        string vertex = queue.Dequeue();\n        result.Add(vertex);\n        foreach (var neighbor in graph.GetNeighbors(vertex)) {\n            if (!visited.Contains(neighbor)) {\n                visited.Add(neighbor);\n                queue.Enqueue(neighbor);\n            }\n        }\n    }\n    return result;\n}\n\nvar bfsResult = BFS(g, \"A\");\nConsole.WriteLine(string.Join(\", \", bfsResult));</code></pre><h2>DFS (Depth-First Search)</h2><pre><code class="language-csharp">List&lt;string&gt; DFS(Graph graph, string start,\n    HashSet&lt;string&gt; visited = null) {\n    if (visited == null) visited = new HashSet&lt;string&gt;();\n    visited.Add(start);\n    var result = new List&lt;string&gt; { start };\n    foreach (var neighbor in graph.GetNeighbors(start)) {\n        if (!visited.Contains(neighbor))\n            result.AddRange(DFS(graph, neighbor, visited));\n    }\n    return result;\n}\n\nvar dfsResult = DFS(g, \"A\");\nConsole.WriteLine(string.Join(\", \", dfsResult));</code></pre>',cz:'<h1>Grafy</h1><p>Graf se skládá z vrcholů a hran. Používáme <strong>seznam sousedů</strong> pro efektivní reprezentaci.</p><h2>Graf se seznamem sousedů</h2><pre><code class="language-csharp">class Graf {\n    private Dictionary&lt;string, List&lt;string&gt;&gt; adj\n        = new Dictionary&lt;string, List&lt;string&gt;&gt;();\n\n    public void PridejHranu(string u, string v) {\n        if (!adj.ContainsKey(u)) adj[u] = new List&lt;string&gt;();\n        if (!adj.ContainsKey(v)) adj[v] = new List&lt;string&gt;();\n        adj[u].Add(v);\n        adj[v].Add(u);\n    }\n\n    public List&lt;string&gt; Sousede(string v) {\n        return adj.ContainsKey(v) ? adj[v] : new List&lt;string&gt;();\n    }\n}\n\nvar g = new Graf();\ng.PridejHranu(\"A\", \"B\");\ng.PridejHranu(\"A\", \"C\");\ng.PridejHranu(\"B\", \"D\");\ng.PridejHranu(\"D\", \"E\");</code></pre><h2>BFS (prohledávání do šířky)</h2><pre><code class="language-csharp">List&lt;string&gt; BFS(Graf graf, string start) {\n    var navstivene = new HashSet&lt;string&gt;();\n    var fronta = new Queue&lt;string&gt;();\n    var vysledek = new List&lt;string&gt;();\n    fronta.Enqueue(start);\n    navstivene.Add(start);\n\n    while (fronta.Count > 0) {\n        string v = fronta.Dequeue();\n        vysledek.Add(v);\n        foreach (var s in graf.Sousede(v)) {\n            if (!navstivene.Contains(s)) {\n                navstivene.Add(s);\n                fronta.Enqueue(s);\n            }\n        }\n    }\n    return vysledek;\n}\n\nConsole.WriteLine(string.Join(\", \", BFS(g, \"A\")));</code></pre><h2>DFS (prohledávání do hloubky)</h2><pre><code class="language-csharp">List&lt;string&gt; DFS(Graf graf, string start,\n    HashSet&lt;string&gt; navstivene = null) {\n    if (navstivene == null) navstivene = new HashSet&lt;string&gt;();\n    navstivene.Add(start);\n    var vysledek = new List&lt;string&gt; { start };\n    foreach (var s in graf.Sousede(start)) {\n        if (!navstivene.Contains(s))\n            vysledek.AddRange(DFS(graf, s, navstivene));\n    }\n    return vysledek;\n}\n\nConsole.WriteLine(string.Join(\", \", DFS(g, \"A\")));</code></pre>'}}},
{id:40,hackathon:true,difficulty:5,title:{python:{en:'Hackathon: Algorithm Challenge',cz:'Hackathon: Algoritmická výzva'},csharp:{en:'Hackathon: Algorithm Challenge',cz:'Hackathon: Algoritmická výzva'}},description:{python:{en:'Solve problems with memoization and tabulation',cz:'Řeš problémy pomocí memoizace a tabulace'},csharp:{en:'Solve problems with memoization and tabulation',cz:'Řeš problémy pomocí memoizace a tabulace'}},content:{python:{en:'<h1>Dynamic Programming</h1><p>Dynamic programming (DP) solves complex problems by breaking them into overlapping subproblems. Two approaches: <strong>memoization</strong> (top-down) and <strong>tabulation</strong> (bottom-up).</p><h2>Fibonacci with Memoization</h2><p>Store computed results in a dictionary to avoid redundant calculations:</p><pre><code class="language-python">def fib_memo(n, memo={}):\n    if n in memo:\n        return memo[n]\n    if n &lt;= 1:\n        return n\n    memo[n] = fib_memo(n - 1, memo) + fib_memo(n - 2, memo)\n    return memo[n]\n\nprint(fib_memo(10))  # 55\nprint(fib_memo(50))  # 12586269025</code></pre><h2>Fibonacci with Tabulation</h2><p>Build the solution bottom-up using a table:</p><pre><code class="language-python">def fib_tab(n):\n    if n &lt;= 1:\n        return n\n    dp = [0] * (n + 1)\n    dp[1] = 1\n    for i in range(2, n + 1):\n        dp[i] = dp[i - 1] + dp[i - 2]\n    return dp[n]\n\nprint(fib_tab(10))  # 55\nprint(fib_tab(100)) # 354224848179261915075</code></pre><h2>Coin Change Problem</h2><p>Find the minimum number of coins to make a given amount:</p><pre><code class="language-python">def coin_change(coins, amount):\n    dp = [float(\"inf\")] * (amount + 1)\n    dp[0] = 0\n\n    for i in range(1, amount + 1):\n        for coin in coins:\n            if coin &lt;= i and dp[i - coin] + 1 &lt; dp[i]:\n                dp[i] = dp[i - coin] + 1\n\n    return dp[amount] if dp[amount] != float(\"inf\") else -1\n\nprint(coin_change([1, 5, 10, 25], 36))  # 3 (25+10+1)\nprint(coin_change([2], 3))              # -1</code></pre><div class="warning"><p><strong>Warning:</strong> Naive recursion without memoization can be extremely slow (exponential time). Always use DP for overlapping subproblems.</p></div>',cz:'<h1>Dynamické programování</h1><p>Dynamické programování (DP) řeší složité problémy rozdělením na překrývající se podproblémy. Dva přístupy: <strong>memoizace</strong> (shora dolů) a <strong>tabulace</strong> (zdola nahoru).</p><h2>Fibonacci s memoizací</h2><p>Ulož vypočítané výsledky do slovníku:</p><pre><code class="language-python">def fib_memo(n, memo={}):\n    if n in memo:\n        return memo[n]\n    if n &lt;= 1:\n        return n\n    memo[n] = fib_memo(n - 1, memo) + fib_memo(n - 2, memo)\n    return memo[n]\n\nprint(fib_memo(10))  # 55\nprint(fib_memo(50))  # 12586269025</code></pre><h2>Fibonacci s tabulací</h2><p>Postav řešení zdola nahoru pomocí tabulky:</p><pre><code class="language-python">def fib_tab(n):\n    if n &lt;= 1:\n        return n\n    dp = [0] * (n + 1)\n    dp[1] = 1\n    for i in range(2, n + 1):\n        dp[i] = dp[i - 1] + dp[i - 2]\n    return dp[n]\n\nprint(fib_tab(10))  # 55\nprint(fib_tab(100)) # 354224848179261915075</code></pre><h2>Problém mincí</h2><p>Najdi minimální počet mincí pro danou částku:</p><pre><code class="language-python">def mince(hodnoty, castka):\n    dp = [float(\"inf\")] * (castka + 1)\n    dp[0] = 0\n\n    for i in range(1, castka + 1):\n        for mince in hodnoty:\n            if mince &lt;= i and dp[i - mince] + 1 &lt; dp[i]:\n                dp[i] = dp[i - mince] + 1\n\n    return dp[castka] if dp[castka] != float(\"inf\") else -1\n\nprint(mince([1, 5, 10, 25], 36))  # 3 (25+10+1)\nprint(mince([2], 3))              # -1</code></pre><div class="warning"><p><strong>Upozornění:</strong> Naivní rekurze bez memoizace může být extrémně pomalá (exponenciální čas). Vždy používej DP pro překrývající se podproblémy.</p></div>'},csharp:{en:'<h1>Dynamic Programming</h1><p>Dynamic programming (DP) solves complex problems by breaking them into overlapping subproblems. Two approaches: <strong>memoization</strong> (top-down) and <strong>tabulation</strong> (bottom-up).</p><h2>Fibonacci with Memoization</h2><p>Store results in a Dictionary to avoid redundant calculations:</p><pre><code class="language-csharp">Dictionary&lt;int, long&gt; memo = new Dictionary&lt;int, long&gt;();\n\nlong FibMemo(int n) {\n    if (memo.ContainsKey(n)) return memo[n];\n    if (n &lt;= 1) return n;\n    memo[n] = FibMemo(n - 1) + FibMemo(n - 2);\n    return memo[n];\n}\n\nConsole.WriteLine(FibMemo(10));  // 55\nConsole.WriteLine(FibMemo(50));  // 12586269025</code></pre><h2>Fibonacci with Tabulation</h2><p>Build the solution bottom-up:</p><pre><code class="language-csharp">long FibTab(int n) {\n    if (n &lt;= 1) return n;\n    long[] dp = new long[n + 1];\n    dp[0] = 0;\n    dp[1] = 1;\n    for (int i = 2; i &lt;= n; i++)\n        dp[i] = dp[i - 1] + dp[i - 2];\n    return dp[n];\n}\n\nConsole.WriteLine(FibTab(10));  // 55\nConsole.WriteLine(FibTab(50));  // 12586269025</code></pre><h2>Coin Change Problem</h2><p>Find the minimum number of coins for a given amount:</p><pre><code class="language-csharp">int CoinChange(int[] coins, int amount) {\n    int[] dp = new int[amount + 1];\n    Array.Fill(dp, int.MaxValue);\n    dp[0] = 0;\n\n    for (int i = 1; i &lt;= amount; i++) {\n        foreach (int coin in coins) {\n            if (coin &lt;= i &amp;&amp; dp[i - coin] != int.MaxValue)\n                dp[i] = Math.Min(dp[i], dp[i - coin] + 1);\n        }\n    }\n    return dp[amount] == int.MaxValue ? -1 : dp[amount];\n}\n\nConsole.WriteLine(CoinChange(new[]{1,5,10,25}, 36)); // 3\nConsole.WriteLine(CoinChange(new[]{2}, 3));           // -1</code></pre><div class="warning"><p><strong>Warning:</strong> Naive recursion without memoization is extremely slow (exponential time). Always use DP for overlapping subproblems.</p></div>',cz:'<h1>Dynamické programování</h1><p>Dynamické programování (DP) řeší složité problémy rozdělením na překrývající se podproblémy. Dva přístupy: <strong>memoizace</strong> (shora dolů) a <strong>tabulace</strong> (zdola nahoru).</p><h2>Fibonacci s memoizací</h2><p>Ulož výsledky do Dictionary:</p><pre><code class="language-csharp">Dictionary&lt;int, long&gt; memo = new Dictionary&lt;int, long&gt;();\n\nlong FibMemo(int n) {\n    if (memo.ContainsKey(n)) return memo[n];\n    if (n &lt;= 1) return n;\n    memo[n] = FibMemo(n - 1) + FibMemo(n - 2);\n    return memo[n];\n}\n\nConsole.WriteLine(FibMemo(10));  // 55\nConsole.WriteLine(FibMemo(50));  // 12586269025</code></pre><h2>Fibonacci s tabulací</h2><p>Postav řešení zdola nahoru:</p><pre><code class="language-csharp">long FibTab(int n) {\n    if (n &lt;= 1) return n;\n    long[] dp = new long[n + 1];\n    dp[0] = 0;\n    dp[1] = 1;\n    for (int i = 2; i &lt;= n; i++)\n        dp[i] = dp[i - 1] + dp[i - 2];\n    return dp[n];\n}\n\nConsole.WriteLine(FibTab(10));  // 55</code></pre><h2>Problém mincí</h2><p>Najdi minimální počet mincí pro danou částku:</p><pre><code class="language-csharp">int Mince(int[] hodnoty, int castka) {\n    int[] dp = new int[castka + 1];\n    Array.Fill(dp, int.MaxValue);\n    dp[0] = 0;\n\n    for (int i = 1; i &lt;= castka; i++) {\n        foreach (int m in hodnoty) {\n            if (m &lt;= i &amp;&amp; dp[i - m] != int.MaxValue)\n                dp[i] = Math.Min(dp[i], dp[i - m] + 1);\n        }\n    }\n    return dp[castka] == int.MaxValue ? -1 : dp[castka];\n}\n\nConsole.WriteLine(Mince(new[]{1,5,10,25}, 36)); // 3\nConsole.WriteLine(Mince(new[]{2}, 3));           // -1</code></pre><div class="warning"><p><strong>Upozornění:</strong> Naivní rekurze bez memoizace je extrémně pomalá. Vždy používej DP pro překrývající se podproblémy.</p></div>'}}}
];
